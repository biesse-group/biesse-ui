<!-- Iconography.stories.mdx -->

import { IconGallery, IconItem, Meta, Title } from "@storybook/addon-docs/";

import { Icon } from "../components";
import iconsMap from "../components/Icon/icons-map";

<Meta title="Icons/Iconography" />

# Iconography

<IconGallery>
  {Object.keys(iconsMap).map((iconName) => (
    <IconItem name={iconName.replace(/-/g, " ")}>
      <Icon size="small" name={iconName} />
    </IconItem>
  ))}
</IconGallery>
